services:
  scalerbot:
    build:
      context: .
    env_file:
      - .env.production
    networks:
      - shared-mongo-net
    restart: unless-stopped

networks:
  shared-mongo-net:
    external: true
